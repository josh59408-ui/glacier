# 台股技術分析篩選系統依賴清單
# 使用 HybridClient (yfinance + FinMind) + SQLite 架構
# 需要 Python 3.10+

# 資料處理
pandas>=2.0.0
numpy>=1.24.0

# 股票資料 API
yfinance>=1.0.0,<2.0.0  # 已驗證相容 1.0~1.4；2.x 為避免破壞變更先擋住
lxml>=5.0.0  # yfinance 網頁解析需要
requests>=2.31.0  # FinMind API 呼叫

# 資料庫
sqlalchemy>=2.0.0
# SQLite 內建於 Python，無需額外安裝

# Google Sheet
gspread>=5.12.0
google-auth>=2.23.0

# 排程（本地使用）
schedule>=1.2.0

# 環境變數
python-dotenv>=1.0.0

# 日誌
loguru>=0.7.0

# 開發工具
pytest>=7.4.0
pytest-cov>=4.1.0
black>=23.0.0
ruff>=0.1.0
